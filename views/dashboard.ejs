<%- include('partials/_header'); %>

<body class="inter-my">
    <%- include('partials/_navigation'); %>

    <div class="container mt-4 text-center">
        <div class="row mt-5 justify-content-center">
            <div class="col-2 dash-cover-timeToLearn rounded-1 d-flex flex-column me-2 p-2">
                <div>Time to Repeat</div>
                <div class="d-flex align-items-center p-1">
                    <i class="fa-solid fa-dumbbell fa-3x me-1"></i>
                    <div>50</div>
                </div>
            </div>
            <div class="col-2 dash-cover-writed rounded-1 d-flex flex-column me-2 p-2">
                <div>Writed</div>
                <div class="d-flex align-items-center p-1">
                    <i class="fa-solid fa-pencil fa-3x"></i>
                    <div>50</div>
                </div>
            </div>
            <div class="col-2 dash-cover-mastered rounded-1 d-flex flex-column me-2 p-2">
                <div>Mastered</div>
                <div class="d-flex align-items-center p-1">
                    <i class="fa-solid fa-meteor fa-3x"></i>
                    <div>50</div>
                </div>
            </div>
            <div class="col-2 dash-cover-totalWords rounded-1 d-flex flex-column me-2 p-2">
                <div>Total Words</div>
                <div class="d-flex align-items-center p-1">
                    <i class="fa-solid fa-person-hiking fa-3x"></i>
                    <div>50</div>
                </div>
            </div>
        </div>
    </div>
    <div class="container mt-4 mb-4 min-heigh-vh">
        <div class="row mt-5 justify-content-center">
            <div class="col-6 d-flex flex-column justify-content-center">
                <!-- 
                <h2 class="d-flex justify-content-center text-theme-1">Have a good work &nbsp; <span class="text-success">{ </span> <%= username %> <span class="text-success"> }</span> &nbsp !</h2>
                -->
                <div class="dropdown mt-4">
                    <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Period
                    </button>
                    <ul class="dropdown-menu">
                        <li class="point-it"><a id="weekly-drop" class="dropdown-item">Weekly</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li class="point-it"><a id="monthly-drop" class="dropdown-item">Monthly</a></li>
                        <li class="point-it"><a id="yearly-drop" class="dropdown-item">Yearly</a></li>
                    </ul>
                </div>
                <div class="mb-4">
                    <canvas id="weeklyChart" width="400" height="200"></canvas>
                    <canvas id="monthlyChart" class="d-none" width="400" height="200"></canvas>
                    <canvas id="yearlyChart" class="d-none" width="400" height="200"></canvas>
                </div>
            </div>
        </div>
    </div>

<!--     
<canvas id="wordsChart" width="400" height="200"></canvas>



    <script>


        const ctx = document.getElementById('wordsChart');
        new Chart(ctx, {
            type: 'pie',
            data: {
                labels: ['Learned', 'Learning'],
                datasets: [{
                    data: [120, 45], // örnek: 120 kelime öğrenildi, 45 öğreniliyor
                    backgroundColor: ['#28a745', '#ffc107']
                }]
            },
            options: {
            responsive: false,   // büyüyüp küçülmesin
            maintainAspectRatio: false
            }
        });
</script>
-->
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script src="/dashboard.js"></script>
<script type="module" src="/dashboard.js"></script>
<%- include('partials/_footer'); %>
